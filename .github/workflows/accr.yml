on:
  push:
    paths:
    - 'accr/README.md'
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: 拉取仓库
      uses: actions/checkout@v4
    
    - uses: aliyun/acr-login@v1
      with:
        login-server: 'crpi-qqjr4xowoqr9zwh0.cn-beijing.personal.cr.aliyuncs.com'
        username: "${{ secrets.ACCR_USER_NAME }}"
        password: "${{ secrets.ACCR_PASSWORD }}"

    - name: test
      run: |
        docker images
        docker logout
